# ResumeBuilder

A simple resume builder application built with React and TypeScript.
To use the application, simply clone the repository and run the following commands:

```bash
npm install
npm run dev
```
