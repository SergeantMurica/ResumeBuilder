export interface ResumeSection {
  id: string;
  type: string;
  title: string;
  data: any;
  position: number;
}

export interface Theme {
  primaryColor: string;
  secondaryColor: string;
  textColor: string;
  backgroundColor: string;
  fontFamily: string;
  fontSize: string;
  borderRadius: string;
  spacing: string;
}
